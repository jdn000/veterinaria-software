<h1>Mis mascotas</h1>

<table>
  <tr>

    <th>Nombre  </th>
    <th>Tipo </th>
    <th>Sexo </th>
    <th>Raza </th>
    <th>Edad  </th>
    <th colspan="3"></th>
  </tr>
 
<% @pets.each do |pet| %>
  <tr>  
      <td><%= pet.nombre %></td>
      <td><%= pet.tipo_mascota %></td>
      <td><%= pet.sexo %></td>
      <td><%= pet.raza %></td>
      <td><%= pet.edad %> años</td>
    <td><%= link_to 'Ver ', user_pet_path(id:pet.id), :class=>"btn btn-primary"%></td>
    <td><%= link_to 'Editar', edit_user_pet_path(id:pet.id), :class=>"btn btn-primary btn-sm"%></td>
  </tr>
<% end %>
</table>
<%=link_to 'Nueva Mascota',new_user_pet_path(current_user.id),:class=>"btn btn-primary btn-sm" %>
