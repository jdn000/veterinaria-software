<% if @attention.errors.empty? %>
	// add the row to the table, show alert and hide modal
	$('#container').append(' <%= j render "index", attention: @attention %>');
	$('#attention-modal').remove();
	$('.modal-backdrop').remove();
	$('.alert-success').html("El proveedor ha sido creado con éxito").show();
<% else %>
	<% puts "Hay un error en formulario" %>
	console.log("<%= @attention.errors.messages %>")
	error = document.getElementById('modal-errores');
	error.innerHTML = '<%=  j render "error" %>';
	error.style.display = "block";
	// $('#modal-errores').html('<%= j render "error" %>');
<% end %>
